#!/usr/bin/env bash

# Experimental script to run Claude Code with Fence

fence \
	--settings ~/.fence.json \
	${FENCE_MONITOR:+--monitor} \
	${FENCE_DEBUG:+--debug} \
	-- \
	claude \
	--dangerously-skip-permissions \
	--strict-mcp-config --mcp-config '{"mcpServers": {}}' \
	"$@"
